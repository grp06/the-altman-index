services:
  - type: web
    name: the-altman-index
    runtime: python
    plan: starter
    region: oregon
    branch: main
    buildCommand: pip install uv && uv sync --frozen --no-dev --project apps/backend
    startCommand: uv run --project apps/backend uvicorn rag_backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHONPATH
        value: /opt/render/project/src/apps/backend/src:/opt/render/project/src/libs/python/core/src
      - key: RAG_BACKEND_CONFIG_PATH
        value: config/backend.yaml
      - key: OPENAI_API_KEY
        sync: false

